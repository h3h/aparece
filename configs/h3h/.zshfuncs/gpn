# git push new (branch to origin)
function gpn() {
  local default=$(gdb)
  local branch=$(gcb)
  if [[ "$branch" == "$default" ]]; then
    echo "You're on $default. Try again."
    return 1
  fi
  git push -u origin "$branch"
}
