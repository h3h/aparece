# git finish branch: switch back to default, pull changes, delete merged local branch
function gfb() {
  local default=$(gdb)
  git checkout "$default" && git pull && git fetch --prune && \
  for b in $(git branch --merged "$default" | grep -v "$default"); do
    git branch -d "$b"
  done
}
